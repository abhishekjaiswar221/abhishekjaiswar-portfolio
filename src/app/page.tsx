import React, { FC } from "react";
import Hero from "@/components/hero";

const Home: FC = () => {
  return (
    <main>
      <section id="hero">
        <Hero />
      </section>
    </main>
  );
};

export default Home;
